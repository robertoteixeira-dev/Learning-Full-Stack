'use strict'

//Si hacemos algo y en la consola nos dice que hay un error, utilizamos el try catch
//Ponemos el codigo dentro del try catch
try {
    var year = 2019;

    //Va a decir que hay un erro: yeaa
    alert(yeaa);

    //Va a decir que hay un error en la URL
    console.log(decodeURIComponent("https://5%victorroblesweb.es"))

} catch (error) {
    console.log(error)
}







