const user = {
    firstName: 'Roberto',
    age: 25,
    skills: ['PHP', 'JS'],
}

const { firstName, age } = user;

console.log(firstName);

// Em um Array

const [ primary, secondary ] = skills;



